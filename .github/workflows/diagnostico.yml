name: 🕵️‍♂️ Rodar Diagnóstico

on:
  workflow_dispatch: # Isso cria o botão de play manual

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Instalar Dependências (se necessário)
        run: npm install

      - name: 🕵️‍♂️ EXECUTAR DIAGNÓSTICO
        run: node scripts/debug_mvp.js
